[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-room-persist"
version = "0.1.0"
description = "JSONL room event journal — enter, leave, message, state persistence"
license = {text = "MIT"}
requires-python = ">=3.8"
